server:
  port: 8050
        
zuul:
  prefix: /api
  routes:
    user-service:
      path: /user-service/**
      service-id:  user-service
    task-service:
      path: /task-service/**
      service-id: task-service

#Eurkha
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8075/eureka/